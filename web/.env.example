VITE_GATE_URL=wss://localhost:4114
VITE_GATE_HTTP_URL=https://localhost:4114
# Serwis obrazków (widok images/admin) — przeglądarka łączy się bezpośrednio.
# W dockerze wystawiony na hoście na 5279 (kontener 5179). Dev (vite): jeśli
# image chodzi w dockerze, ustaw na http://localhost:5279/api.
VITE_IMAGE_URL=http://localhost:5279/api

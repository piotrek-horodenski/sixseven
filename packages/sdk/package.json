{
  "name": "sixseven-sdk",
  "version": "0.0.1",
  "description": "SDK dla twórców gier sixseven: typy wire contract, serwer deweloperski (serve) i harness kontrakt-testów (test). Szkielet (etap 0); pełne serve/test w etapie 2.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "sixseven-sdk": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "license": "ISC",
  "dependencies": {
    "sixseven-hmac": "*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.10.2",
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  }
}

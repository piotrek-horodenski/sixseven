import { describe, it, expect } from "vitest";

/**
 * Szkielet testów games (etap 0). Sprawdza tylko, że warstwa serwisu jest
 * podpięta. Testy silnika meczu i inwariantów sekretu (I1–I5, S2/S3)
 * dochodzą w etapie 2.
 */
describe("games skeleton", () => {
  it("stała wersja etapu", () => {
    expect(0).toBe(0);
  });
});
